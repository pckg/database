language: php
php:
  - '7.0'
node_js:
  - "0.10"
install:
  # Install Apache
  # - sudo apt-get update -qq
  # - sudo apt-get install -y --force-yes apache2 libapache2-mod-php7.0 php-curl php-mysql php-intl > /dev/null
  # - sudo chmod 777 -R /home/travis/build/redCOMPONENT-COM/redSHOP/
  # - sudo ln -s /home/travis/build/redCOMPONENT-COM/redSHOP/tests/system/joomla-cms/ /var/www/joomla-cms #Add     joomla to apache htdocs
  # - sudo sed -i -e "s,AllowOverride[ ]None,AllowOverride All,g" /etc/apache2/sites-available/default
  # - sudo /etc/init.d/apache2 restart
  # - composer install --dev --prefer-dist

  # XVFB
  - "export DISPLAY=:99.0"
  - "Xvfb :99.0 -extension RANDR > /dev/null &"

  #- "export DISPLAY=:99.0"
  #- "sh -e /etc/init.d/xvfb start"
  #- sleep 3 # give xvfb some time to start

  # Window manager
  #- echo "c10"
  #- "sudo apt-get install fluxbox -y --force-yes"
  #- echo "c11"
  #- "fluxbox &"

  # Selenium server
  - "wget http://selenium-release.storage.googleapis.com/2.53/selenium-server-standalone-2.53.1.jar"
  - "sudo java -jar selenium-server-standalone-2.53.1.jar > /dev/null 2>/dev/null &"
  #- sleep 30 # gives enough time to Selenium to start
  - "php www/index.php -S 127.0.0.1:8888 -t . >/dev/null 2>&1 &"
  # Start up the webdriver.
  - phantomjs --webdriver=4444 >/dev/null 2>&1 &
  - composer install --prefer-dist --optimize-autoloader
  - "wget http://codeception.com/codecept.phar"
  - "cp ./codeception.sample.travis.yml ./codeception.yml"
  #- "cp ./tests/acceptance.suite.sample.travis.yml ./tests/acceptance.suite.yml"
script:
#  - php ./vendor/codecept.phar build
#  - php ./vendor/codecept.phar run acceptance
  - php ./codecept.phar run
#deploy:
#  skip_cleanup: true
addons:
  hosts:
    - derive.travis
sudo: true
cache:
  bundler: true
  directories:
    - vendor
    - bower_components
    - components
    - $HOME/.composer/cache
    - $HOME/.npm
    - $HOME/.cache # includes bowers cache